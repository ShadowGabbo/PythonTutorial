#casual number
import random
from math import sqrt #import not a module but only a function
from math import * #import all the function 

for num in range(10):
    valore = random.randint(1,50)
    print(valore)

print(sqrt(25))

